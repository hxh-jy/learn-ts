/**
 * 自动编译命令
 *     tsc 文件名 -w
 * 自动创建tsconfig.json文件
 *     tsc --init 
 * 自动创建了tsconfig.json文件后 
 * 执行tsc 命令就可以将当前目录下的所有ts文件编译为js
 * 其他的编译信息看tsconfig.json 文件
*/
let string: string = 'asdf'
let test: string = 'test'